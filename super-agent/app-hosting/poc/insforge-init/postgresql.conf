# PostgreSQL configuration for InsForge (App Host POC)
listen_addresses = '*'
wal_level = logical
max_replication_slots = 10
max_wal_senders = 10
shared_preload_libraries = 'pg_cron,http,pgcrypto'
cron.database_name = 'insforge'
