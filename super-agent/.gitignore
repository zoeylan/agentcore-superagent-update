.DS_Store
*.code-workspace
.kiro/skills
super-agent-workspaces/
docs/

# CDK synthesis output (contains AWS account IDs, IAM role ARNs, templates)
infra/cdk.out/

# CDK context cache (contains VPC IDs, subnet IDs, account IDs)
infra/cdk.context.json

# Frontend production environment (contains Cognito secrets and IPs)
frontend/.env.production
**/cdk.out/
**/cdk.context.json
